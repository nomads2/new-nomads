<!DOCTYPE html>

// Background layer for user interface, with applied gausian blurs and rasterized . See Collab site for image sketch

<html lang="en">
 <head>
  <meta charset="UTF-8" />
  <title>CeilingFloatsAway_NOMADSInterface2_basic</title>
  <script>

    function init() {

      var canvas = document.getElementById("canvas");
      var ctx = canvas.getContext("2d");

      draw(ctx);
    }

    function draw(ctx) {

      var alpha = ctx.globalAlpha;

      // layer1/Path
      ctx.save();
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image1"), -0.2, -0.7);

      // layer1/Type

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image2"), 14.0, 15.6);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image3"), 52.7, 183.2);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image4"), 162.7, 182.6);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image5"), 195.9, 80.4);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image6"), 107.9, 14.4);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image7"), 18.7, 80.2);

      // layer1/Type

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image8"), 177.1, 13.6);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image9"), 333.1, 126.8);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image10"), 21.1, 127.3);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image11"), 80.8, 308.5);

      // layer1/Path
      // This unsupported artwork has been rasterized
      ctx.drawImage(document.getElementById("image12"), 271.0, 308.6);
      ctx.restore();
    }
  </script>
 </head>
 <body onload="init()">
   <canvas id="canvas" width="465" height="465"></canvas>
   <img alt="Path" id="image1" style="display: none" src="image15.png" />
   <img alt="Path" id="image2" style="display: none" src="image16.png" />
   <img alt="Path" id="image3" style="display: none" src="image17.png" />
   <img alt="Path" id="image4" style="display: none" src="image18.png" />
   <img alt="Path" id="image5" style="display: none" src="image19.png" />
   <img alt="Path" id="image6" style="display: none" src="image20.png" />
   <img alt="Path" id="image7" style="display: none" src="image21.png" />
   <img alt="Path" id="image8" style="display: none" src="image22.png" />
   <img alt="Path" id="image9" style="display: none" src="image23.png" />
   <img alt="Path" id="image10" style="display: none" src="image24.png" />
   <img alt="Path" id="image11" style="display: none" src="image25.png" />
   <img alt="Path" id="image12" style="display: none" src="image26.png" />
 </body>
</html>
